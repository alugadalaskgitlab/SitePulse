One Stores landing page. Make /stores/hub the only real Stores home. The old /stores page can stay as an internal view (Current Stock / Bulk Materials / Store Items), but strip its own duplicate dashboard header and navigation, and give it a clear "Back to Stores Hub" link — it should never look like a second dashboard.
Make "Pending Store Receipts" actually filter. The GRN History page must read the ?piSourced=true parameter on load and show only PI-sourced, awaiting-verification, non-cancelled, non-finalized records — not the full list.
Simplify GRN status filtering to one clear control — All / Pending Store Receipt / Draft Manual GRN / Awaiting PI / Finalised / Cancelled — replacing the current mix of separate chips plus a dropdown. Supplier/item/date/site filters can stay separate.
Fix the pending GRN card's confusing details: show "Linked PI: HLC/PI/2026/0003" as a clickable link instead of a cryptic short code; only show a destination (Main Store / Plant Store / Site name) when one actually applies — never "No site assigned" for a normal Store receipt; collapse the four overlapping badges into one clear status, "Awaiting Stores Verification."
Fix the cancellation warning wording — a still-draft pending receipt hasn't touched stock yet, so its cancellation message should say so, not warn about removing stock that was never added.
Fix error handling everywhere it's this shallow — both Cancel and Finalize (and any other mutation with the same pattern) must show the real server error message, and finalization failure must keep the user on the GRN screen — never navigate away and surface a vague, delayed toast elsewhere.
Enforce one shared item identity. Before a new Store Item can be created from a PI or GRN, require selecting from the existing Item Catalogue if a close match exists (with suggestions shown), and only allow creating a new catalogue entry after explicit confirmation — so "Ink Cartridges – Color" and "printer ink cartridge" can't silently become two different stock items again.