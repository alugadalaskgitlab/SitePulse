Batch 4: Equipment Hub fixes

Combined-source Equipment Hub KPIs, not duplicate data entry. Equipment usage should keep being logged inside DPRs for road-site work — Site Engineers should not have to enter it twice. Please update Equipment Hub's "Active Today" KPI (and any related reporting) to combine equipment usage from both sources: equipment embedded in submitted DPRs, and standalone Equipment Usage Log records. Site Engineers continue entering equipment only once, inside their DPR; the hub's numbers should reflect the full picture from both places.
Fix "Breakdowns" KPI miscount. In EquipmentHub.tsx, change the filter from m.status === "breakdown" || m.status === "pending" || m.status === "open" to m.eventType === "breakdown" && m.status === "open", so the count reflects actual breakdowns only, not scheduled service/PM records that happen to also be "open."
Fix permission mismatch between Equipment Hub's route and its own tiles. The hub route (App.tsx) is gated by equipment_hub, but nearly every tile and KPI inside is gated by plant_equipment instead. Anyone who can reach the hub should see its core content. Please align these — either the route should check equipment_hub OR plant_equipment (matching the gatedEither pattern used for Masters), or the two permissions should be consolidated into one for core equipment tiles, keeping plant_equipment only for anything genuinely HMP/RMC-plant-specific.
Data check needed before any Generator Logs decision — please query the generator_logs table in both dev and production and report back: how many rows have sourceHeatingSessionId filled in (auto-derived from Heating Sessions) versus how many have it blank (manually entered, independent of any heating session). Do not change anything about Generator Logs yet — this is a read-only check to inform the next decision. If nearly all rows are heating-session-derived, we'll keep that backend link intact but hide the standalone manual Generator Logs tile/form from Equipment Hub, since normal generator usage can go through Equipment Usage Log instead. If a meaningful number are manual/independent entries, we'll need to migrate that use case into Equipment Usage Log properly before touching anything.