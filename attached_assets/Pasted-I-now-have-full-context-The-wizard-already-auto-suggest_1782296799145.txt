I now have full context. The wizard **already** auto-suggests categories from item codes and has "Set unassigned"/"Set all" bulk buttons — what's missing is *subset selection*. Let me write both patches.
Action: create_file({"file_text": "# SitePulse \u2014 BOQ screen: Mapping Confidence badge + \"Re-map All\" CTA\n\nAdds two demo-friendly touches to the BOQ project screen so a prospect instantly\nsees the BOQ \u2192 SNL intelligence working:\n\n1. **\"Re-map All\" button** in the project header (next to *Import BOQ*) \u2014 one click\n   re-runs auto-mapping for every item (`POST /api/boq/projects/:id/remap`, which\n   already exists). No more digging into the floating SNL panel.\n2. **Average confidence badge** inside the existing \"SNL Mapped\" stat tile \u2014 shows the\n   mean auto-match confidence (e.g. `94% avg confidence`), the visible proof that the\n   engine is matching intelligently.\n\nAll changes are in **`client/src/pages/BoqProjectDetail.tsx`** only. No backend or\nschema change \u2014 the `/items` endpoint already returns `snlConfidence` and\n`mappingStatus` per item.\n\n---\n\n## EDIT 1 \u2014 add the `Sparkles` icon to the lucide import\n\n**FIND:**\n\n```tsx\n  Link2, Link2Off, Clock, RefreshCw, Search, CheckCircle2, X,\n} from \"lucide-react\";\n```\n\n**REPLACE WITH:**\n\n```tsx\n  Link2, Link2Off, Clock, RefreshCw, Search, CheckCircle2, X, Sparkles,\n} from \"lucide-react\";\n```\n\n---\n\n## EDIT 2 \u2014 add a page-level re-map mutation\n\n**FIND:**\n\n```tsx\n  // \u2500\u2500 Derived values \u2500\u2500\n  const groupedByWorkCat = useMemo(() => {\n```\n\n**REPLACE WITH:**\n\n```tsx\n  const remapMutation = useMutation({\n    mutationFn: () => apiRequest(\"POST\", `/api/boq/projects/${projectId}/remap`, {}),\n    onSuccess: async () => {\n      toast({ title: \"Re-mapping BOQ \u2192 SNL\u2026\", description: \"Auto-matching items against the Standard Norms Library.\" });\n      await new Promise((r) => setTimeout(r, 1200));\n      await refetchItems();\n      toast({ title: \"Re-map complete \u2713\" });\n    },\n    onError: () => toast({ title: \"Re-map failed\", variant: \"destructive\" }),\n  });\n\n  // \u2500\u2500 Derived values \u2500\u2500\n  const groupedByWorkCat = useMemo(() => {\n```\n\n---\n\n## EDIT 3 \u2014 add the \"Re-map All\" button in the header\n\n**FIND:**\n\n```tsx\n          <Button\n            variant=\"outline\"\n            size=\"sm\"\n            className=\"border-blue-200 text-blue-700 hover:bg-blue-50 h-8\"\n            onClick={() => setShowImport(true)}\n            data-testid=\"button-import-boq-header\"\n          >\n            <Upload className=\"w-3.5 h-3.5 mr-1.5\" />\n            Import BOQ\n          </Button>\n```\n\n**REPLACE WITH:**\n\n```tsx\n          {items.length > 0 && (\n            <Button\n              variant=\"outline\"\n              size=\"sm\"\n              className=\"border-teal-300 text-teal-700 hover:bg-teal-50 h-8\"\n              onClick={() => remapMutation.mutate()}\n              disabled={remapMutation.isPending}\n              data-testid=\"button-remap-all-header\"\n              title=\"Re-run BOQ \u2192 SNL auto-mapping for all items\"\n            >\n              {remapMutation.isPending\n                ? <Loader2 className=\"w-3.5 h-3.5 mr-1.5 animate-spin\" />\n                : <RefreshCw className=\"w-3.5 h-3.5 mr-1.5\" />}\n              Re-map All\n            </Button>\n          )}\n          <Button\n            variant=\"outline\"\n            size=\"sm\"\n            className=\"border-blue-200 text-blue-700 hover:bg-blue-50 h-8\"\n            onClick={() => setShowImport(true)}\n            data-testid=\"button-import-boq-header\"\n          >\n            <Upload className=\"w-3.5 h-3.5 mr-1.5\" />\n            Import BOQ\n          </Button>\n```\n\n---\n\n## EDIT 4 \u2014 compute average confidence + add `confidence` to the tile type\n\n**FIND:**\n\n```tsx\n        const snlFullyMapped = items.length > 0 && mappedCount === items.length;\n        const snlNeedsAction = items.length > 0 && (reviewCount > 0 || unmappedCount > 0);\n\n        const tiles: { label: string; value: string; extra?: string; extraCls?: string; onClick?: () => void; highlight?: boolean }[] = [\n```\n\n**REPLACE WITH:**\n\n```tsx\n        const snlFullyMapped = items.length > 0 && mappedCount === items.length;\n        const snlNeedsAction = items.length > 0 && (reviewCount > 0 || unmappedCount > 0);\n\n        const autoConfItems = items.filter(i => (i.snlMappingStatus ?? i.mappingStatus) === \"mapped\" && i.snlConfidence != null);\n        const avgConfidence = autoConfItems.length > 0\n          ? Math.round((autoConfItems.reduce((s, i) => s + (i.snlConfidence ?? 0), 0) / autoConfItems.length) * 100)\n          : null;\n\n        const tiles: { label: string; value: string; extra?: string; extraCls?: string; onClick?: () => void; highlight?: boolean; confidence?: number }[] = [\n```\n\n---\n\n## EDIT 5 \u2014 surface the confidence on the \"SNL Mapped\" tile\n\n**FIND:**\n\n```tsx\n            extraCls: snlNeedsAction ? \"text-amber-600\" : \"text-emerald-600\",\n            highlight: snlNeedsAction,\n            onClick: items.length > 0 ? () => setSnlFloatingOpen(true) : undefined,\n          },\n```\n\n**REPLACE WITH:**\n\n```tsx\n            extraCls: snlNeedsAction ? \"text-amber-600\" : \"text-emerald-600\",\n            highlight: snlNeedsAction,\n            onClick: items.length > 0 ? () => setSnlFloatingOpen(true) : undefined,\n            confidence: avgConfidence ?? undefined,\n          },\n```\n\n---\n\n## EDIT 6 \u2014 render the badge\n\n**6a.** Add `confidence` to the tile destructuring.\n\n**FIND:**\n\n```tsx\n            {tiles.map(({ label, value, extra, extraCls, onClick, highlight }) => (\n```\n\n**REPLACE WITH:**\n\n```tsx\n            {tiles.map(({ label, value, extra, extraCls, onClick, highlight, confidence }) => (\n```\n\n**6b.** Render the badge right after the `extra` line.\n\n**FIND:**\n\n```tsx\n                  {extra && <p className={`text-[12px] mt-0.5 ${extraCls ?? \"\"}`}>{extra}</p>}\n```\n\n**REPLACE WITH:**\n\n```tsx\n                  {extra && <p className={`text-[12px] mt-0.5 ${extraCls ?? \"\"}`}>{extra}</p>}\n                  {confidence != null && (\n                    <Badge variant=\"outline\" className=\"mt-1 text-[10px] bg-teal-50 border-teal-200 text-teal-700 inline-flex items-center gap-1 px-1.5 py-0\">\n                      <Sparkles className=\"w-2.5 h-2.5\" /> {confidence}% avg confidence\n                    </Badge>\n                  )}\n```\n\n---\n\n## Verify\n\n1. Restart dev server, open a BOQ project with items.\n2. Header shows **Re-map All** \u2014 click it \u2192 toast \"Re-mapping\u2026\" then \"Re-map complete \u2713\",\n   and the SNL Mapped tile updates.\n3. The **SNL Mapped** tile shows a teal `\u2728 94% avg confidence` badge once items are\n   auto-mapped (only counts auto-matched items; manual maps have no score).\n", "path": "/app/SITEPULSE_BOQ_MAPPING_CONFIDENCE_CTA.md"})
Observation: Create successful: /app/SITEPULSE_BOQ_MAPPING_CONFIDENCE_CTA.md